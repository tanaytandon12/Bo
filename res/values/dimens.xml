<resources>

    <!-- Default screen margins, per the Android Design guidelines. -->
    <dimen name="activity_horizontal_margin">8dp</dimen>
    <dimen name="activity_vertical_margin">8dp</dimen>

    <!-- the padding value for the views -->
    <dimen name="padding">5dp</dimen>

    <dimen name="padding_searchbox">10dp</dimen>
    
    <!-- the dimensions for the paper image -->
    <dimen name="paper_img_width">60dp</dimen>
    <dimen name="paper_img_height">60dp</dimen>

    <!-- the dimension for the paper title -->
    <dimen name="paper_title_txt_size">15sp</dimen>

    <!-- the dimesion for the paper size -->
    <dimen name="paper_size_txt_size">12sp</dimen>

    <!-- the dimesions for the paper type -->
    <dimen name="paper_type_txt_size">15sp</dimen>

    <!-- the font size of the search text -->
    <dimen name="search_txt_size">20sp</dimen>

    <!-- the corners of the search text background -->
    <dimen name="bg_search_txt_corners">2dp</dimen>

    <!-- the font size for no internet buttons -->
    <dimen name="no_internet_txt_size">15sp</dimen>

    <!-- the font size for no results message -->
    <dimen name="no_search_result_txt_size">20sp</dimen>
    <!-- the si image dimension -->
    <dimen name="si_img_width">25dp</dimen>
    <dimen name="si_img_height">25dp</dimen>

</resources>